{% extends 'base.html.twig' %}

{% block title %}New Section{% endblock %}

{% block body %}
    <h1>Create new Section !!!! avec la section d'un formation avant </h1>


    {{ form_start(form) }}
    {{ form_widget(form.title) }}

    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}


    <a href="{{ path('app_section_index') }}">back to list</a>
{% endblock %}
